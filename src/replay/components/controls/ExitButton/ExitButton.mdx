---
name: Exit button
route: /controls/exit-button
menu: 'Controls reference'
---

import { Playground, PropsTable } from 'docz';
import ExitButton from './ExitButton';
import { XCircle } from 'react-feather';
import SimpleTable from '../../../docs/mdx-helpers/SimpleTable';
import '../../../replay-default.css';

# ExitButton

Standalone overlay button for closing the player.

## Summary

Intended to be placed in one of the upper corners of the player. Is used in the default player for providing a way to shut down the player.

## Example

<Playground>
  <div style={{ backgroundColor: 'gray', height: '120px', position: 'relative' }}>
    <ExitButton content={<XCircle/>} onClick={() => console.log('Exit clicked.')} />
  </div>
</Playground>

## Props

<PropsTable of={ExitButton} />

## Component DOM with class names

Please read the [general principles](/custom-replay/skins-styles) for Replay class names and styling.

<SimpleTable rows={[{ 
  'Element': 'div',
  'Class name': 'exit-button',
  'Generic class name': 'button',
  'States': '',
  'Parent': ''
},{
  'Element': 'div',
  'Class name': '',
  'Generic class name': '',
  'States': '',
  'Parent': '.exit-button'
}]}/>
